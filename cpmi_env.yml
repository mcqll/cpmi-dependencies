# This file may be used to create an environment using:
# $ conda create --name <env> --file <this file>
# platform: linux-64
channels:
  - defaults
dependencies:
  - tqdm
  - jupyter_conda
  - torchvision
  - python=3.7
  - cudatoolkit=9.2
  - pandas
  - pytorch
  - numpy
  - nltk

